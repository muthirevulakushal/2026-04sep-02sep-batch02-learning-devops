apiVersion: v1
kind: Service
metadata:
  name: kushal-service
  labels:
    app: kushal-app
spec:
  type: LoadBalancer
  selector:
    app: kushal-app
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP